import 'package:flutter/material.dart';
import 'package:get/get.dart';
import '../index.dart';

class DestinationModalWidget extends GetView<DestinationModalController> {
  const DestinationModalWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: const [],
    );
  }
}
